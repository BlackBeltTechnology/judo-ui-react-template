{{> fragment.header.hbs }}

import type { GridSortModel, GridFilterModel } from '@mui/x-data-grid{{ getMUIDataGridPlanSuffix }}';
import type { Filter } from '../components-api';

export interface PersistedTableData {
  sortModel?: GridSortModel;
  filterModel?: GridFilterModel;
  filters?: Filter[];
}

export interface RefreshableTable<T> {
  fetchData: () => Promise<T[] | undefined>;
}
