{{> fragment.header.hbs }}

export type AlternativeApplicationGenerator = (translateFn: Function) => Record<string, string>;

export type HandleApplicationChange = (applicationKey: string) => void;

export interface AggregationInputButtonProps {
  name: string;
  className: string;
  disabled?: boolean;
  onClick?: (event: Event, handleDropdown: (event: Event) => void) => void;
  title: any;
  iconName: string;
  visible?: (value: any, onClick?: (event: Event, handleDropdown: (event: Event) => void) => void) => boolean;
  dropDownButtonIsVisible?: (value: any, onClick?: (event: Event, handleDropdown: (event: Event) => void) => void) => boolean;
}
