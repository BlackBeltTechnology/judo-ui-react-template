{{> fragment.header.hbs }}

export type AlternativeApplicationGenerator = (translateFn: Function) => Record<string, string>;

export type HandleApplicationChange = (applicationKey: string) => void;

export interface SingleRelationInputButtonProps<T> {
  name: string;
  className: string;
  disabled?: boolean;
  onClick: (value: T | null) => void;
  title: string;
  iconName: string;
  visible?: (value: T | null) => boolean;
  dropDownButtonIsVisible?: (value: T | null) => boolean;
}
