{{> fragment.header.hbs }}

import { Link, Typography } from '@mui/material';

export function Footer() {
  return (
      <Typography id="application-footer" variant="body2" color="text.secondary" align="center">
        {{ modelName application.name }} - {new Date().getFullYear()}.
      </Typography>
  );
}
