import { useTheme } from '@mui/material/styles';

const LogoIcon = () => {
  const theme = useTheme();

  return (
    <img src={'judo-icon.webp'} style={ { height: '100%', maxWidth: '100%', borderRadius: '50%' } } />
  );
};

export default LogoIcon;
