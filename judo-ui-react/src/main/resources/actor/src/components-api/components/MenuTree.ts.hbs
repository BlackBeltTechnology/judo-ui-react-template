{{> fragment.header.hbs }}

export type MenuItem = {
  id: string;
  icon?: string;
  level?: number;
  label: string;
  to?: string;
  items?: MenuItem[];
  hidden?: boolean;
};

export interface MenuProps {
  items: MenuItem[];
  height?: number;
  maxWidth?: number;
  defaultExpanded?: string[];
}
