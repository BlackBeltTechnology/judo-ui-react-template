{{> fragment.header.hbs }}

export type MenuItem = {
  id: string;
  icon?: string;
  label: string;
  to?: string;
  items?: MenuItem[];
};

export interface MenuProps {
  items: MenuItem[];
  height?: number;
  maxWidth?: number;
  defaultExpanded?: string[];
}
