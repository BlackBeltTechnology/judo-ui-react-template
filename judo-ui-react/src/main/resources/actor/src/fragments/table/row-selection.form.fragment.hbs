const [selectionModel, setSelectionModel] = useState<GridRowSelectionModel>([]);
const selectedRows = useRef<{{ classDataName table.dataElement.target 'Stored' }}[]>([]);

useEffect(() => {
  selectedRows.current = getUpdatedRowsSelected(selectedRows, ownerData?.{{ table.dataElement.name }} ?? [], selectionModel);
}, [selectionModel]);
