<Tooltip title={<TooltipContent text={t('{{ getTranslationKeyForVisualElement input }}.tooltip', { defaultValue: '{{ escapeString input.tooltipText }}' })} />} placement="right">
  <IconButton>
    <MdiIcon path="help-circle-outline" />
  </IconButton>
</Tooltip>
