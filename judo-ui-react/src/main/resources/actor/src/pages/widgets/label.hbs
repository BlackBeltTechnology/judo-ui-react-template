{{# if child.hiddenBy }}( !data.{{ child.hiddenBy.name }} && {{/ if }}
<Grid container item alignItems="center" justifyContent="flex-start" {{# unless (isParentDirectionColumn child) }}xs={12}{{/ unless }} {{# neq (calculateSize child) 12.0 }}sm={ {{ calculateSize child }} }{{/ neq }}>
    {{# if child.icon }}
        <MdiIcon path="{{ child.icon.name }}" />
    {{/ if }}
    <Typography variant="h6" component="h1">
        { t('{{ idToTranslationKey child.fQName application }}', { defaultValue: '{{ child.label }}' }) }
    </Typography>
</Grid>
{{# if child.hiddenBy }}){{/ if }}
