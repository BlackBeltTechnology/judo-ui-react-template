// Action: RefreshRelationAction

export type {{ actionFunctionTypeName action }} = () => (fetchData: () => Promise<void>) => Promise<void>;

export const {{ actionFunctionHookName action }}: {{ actionFunctionTypeName action }} = () => {
    return async function {{ actionFunctionName action }}(fetchData: () => Promise<void>) {
        return fetchData();
    }
}
