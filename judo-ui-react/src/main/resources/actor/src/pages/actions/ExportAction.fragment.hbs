// ExportAction: {{ getXMIID action }}
{{# if action.targetDataElement }}
const {{ simpleActionDefinitionName action.actionDefinition }} = async (queryCustomizer: {{ classDataName action.targetDataElement.target 'QueryCustomizer' }}) => {
{{ else }}
const {{ simpleActionDefinitionName action.actionDefinition }} = async (queryCustomizer: {{ classDataName (getReferenceClassType page) 'QueryCustomizer' }}) => {
{{/ if }}
  try {
    setIsLoading(true);
    const response = await runtimeService.exportRelation('{{ deriveClassPath (getReferenceClassType page) }}', '{{# if action.targetDataElement }}{{ action.targetDataElement.name }}{{/ if }}', {{# if (pageHasSignedId page) }}{ __signedIdentifier: signedIdentifier } as any{{ else }}owner.current{{/ if }}, queryCustomizer);

    exportFile(response);
  } catch (error) {
    console.error(error);
  }
  finally {
    setIsLoading(false);
  }
}
