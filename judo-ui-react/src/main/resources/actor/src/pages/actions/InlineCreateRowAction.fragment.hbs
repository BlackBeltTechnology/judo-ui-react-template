// InlineCreateRowAction: {{ getXMIID action }}
const {{ simpleActionDefinitionName action.actionDefinition }} = async (): Promise<{{ classDataName action.actionDefinition.targetType '' }} | undefined> => {
  {{# with action.targetPageDefinition as |form| }}
    {{# with (getTemplateActionForForm form) as |templateAction| }}
      try {
        const { data: result } = await {{ getServiceImplForPage page }}.getTemplate{{# if action.targetDataElement }}For{{ firstToUpper action.targetDataElement.name }}{{/ if }}();
        return result;
      } catch(error) {
        console.error(error);
        return undefined;
      }
    {{/ with }}
  {{/ with }}
};
