{{> fragment.header.hbs }}
{{# unless (hasElements (getUniquePageActions page)) }}
    // page has no actions
    export {};
{{/ unless }}
{{# each (getUniquePageActions page) as |action| }}
    export * from './{{ pageActionPathSuffix action }}';
{{/ each }}
