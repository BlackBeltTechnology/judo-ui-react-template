const {{ simpleActionDefinitionName action.actionDefinition }} = async () => {
  alert('RefreshAction');
};

const {{ simpleActionDefinitionName action.actionDefinition }}ServiceCall = async (queryCustomizer: {{ classDataName (getReferenceClassType page) 'QueryCustomizer' }}): Promise<{{ classDataName (getReferenceClassType page) 'Stored' }}{{# if page.container.table }}[]{{/ if }}> => {
  return Promise.resolve({{# if page.container.table }}[]{{ else }}{} as any{{/ if}});
};
