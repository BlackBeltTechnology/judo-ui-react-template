const {{ simpleActionDefinitionName action.actionDefinition }} = async () => {
  // alert('OpenFormAction');
  const result = await open{{ pageName action.targetPageDefinition }}();
  console.info(result);
};
