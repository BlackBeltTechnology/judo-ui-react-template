{{> fragment.header.hbs }}

import type { MenuItem } from './components-api';

export const menuItems: MenuItem[] = [
    {{# each application.navigationController.items as |item| }}
        {{> actor/src/menu-items-recurse.fragment.hbs item=item  }}
    {{/ each }}
];
