{{> fragment.header.hbs }}

import type { {{ muiDataGridPropsType }}, GridColDef } from '@mui/x-data-grid{{ getMUIDataGridPlanSuffix }}';

export const baseColumnConfig: Partial<GridColDef<any>> = {
    groupable: false,
    hideable: false,
};

export const baseTableConfig: Partial<{{ muiDataGridPropsType }}<any>> = {
    autoHeight: true,
    sortingOrder: ['desc', 'asc'], // omitting null for now, needs further discussion JNG-4826
    pageSizeOptions: [{{ calculateTablePageLimit tablePageLimit }}],
    pagination: true, // is false for DataGridPro by default
    disableColumnFilter: true,
};

export const serverTableConfig: Partial<{{ muiDataGridPropsType }}<any>> = {
    ...baseTableConfig,
    sortingOrder: ['desc', 'asc'],
    sortingMode: 'server',
    paginationMode: 'server',
};

export const pageServerTableConfig: Partial<{{ muiDataGridPropsType }}<any>> = {
    ...serverTableConfig,
    disableRowSelectionOnClick: true,
    filterMode: 'server',
};

export const rangeDialogConfig: { numberOfElements: number } = {
    numberOfElements: 5,
};
