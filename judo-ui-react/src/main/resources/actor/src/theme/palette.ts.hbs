{{> fragment.header.hbs }}

import { createTheme } from '@mui/material/styles';
{{> actor/src/theme/palette.ts.extra-imports.fragment.hbs }}

export const paletteThemeLight = createTheme({
    palette: {
        mode: 'light',
        primary: {
            main: {{# if application.theme.primaryColor }}'{{ application.theme.primaryColor }}'{{ else }}'#3C4166FF'{{/ if }},
        },
        secondary: {
            main: {{# if application.theme.secondaryColor }}'{{ application.theme.secondaryColor }}'{{ else }}'#E7501DFF'{{/ if }},
        },
        text: {
            primary: {{# if application.theme.textPrimaryColor }}'{{ application.theme.textPrimaryColor }}'{{ else }}'#17191DFF'{{/ if }},
            secondary: {{# if application.theme.textSecondaryColor }}'{{ application.theme.textSecondaryColor }}'{{ else }}'#434448FF'{{/ if }},
        },
        background: {
            default: {{# if application.theme.backgroundColor }}'{{ application.theme.backgroundColor }}'{{ else }}'#FAFAFAFF'{{/ if }},
            paper: {{# if application.theme.paperBackgroundColor }}'{{ application.theme.paperBackgroundColor }}'{{ else }}'#FFFFFFFF'{{/ if }},
        },
        subtitleColor: {
            main: {{# if application.theme.subtitleColor }}'{{ application.theme.subtitleColor }}'{{ else }}'#8c8c8c'{{/ if }},
        },
        {{> actor/src/theme/palette.ts.extra-palette-attributes.fragment.hbs }}
    },
    {{> actor/src/theme/palette.ts.extra-attributes.fragment.hbs }}
});

// WIP
export const paletteThemeDark = createTheme({
  palette: {
    mode: 'dark',
    primary: {
      main: '#3c4dcc',
    },
    secondary: {
      main: '#E7501DFF',
    },
    text: {
      primary: '#d9d9d9',
      secondary: '#646464',
    },
    background: {
      default: '#2a2a2a',
    },
    subtitleColor: {
      main: '#8C8C8C',
    },
  },
});

