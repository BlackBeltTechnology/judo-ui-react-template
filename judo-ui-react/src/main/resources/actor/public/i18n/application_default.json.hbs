{
    "translation": {
        "applications.{{ application.actor.simpleName }}": "{{ application.actor.simpleName }}",
    {{# each (getAlternativeApplications application applications) as |app| }}
        "applications.{{ app.actor.simpleName }}": "{{ app.actor.simpleName }}",
    {{/ each }}
    }
}
