# autocomplete-no-results-message

Localize the empty-state message shown by every generated MUI Autocomplete when the search returns zero results, replacing MUI's hard-coded English default `"No options"` with a `judo.autocomplete.no-results` i18n key.
