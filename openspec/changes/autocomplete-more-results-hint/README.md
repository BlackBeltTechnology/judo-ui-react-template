# autocomplete-more-results-hint

Render a non-selectable status footer below the listbox in server-paginated MUI Autocomplete dropdowns, reporting how many results are shown when the returned options list reaches the limit injected by the caller.
